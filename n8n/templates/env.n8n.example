# n8n tool workflow template vars (no secrets)

# Optional source for allowlist placeholder in workflow Set nodes
WEB_ALLOWLIST=example.com,localhost

# HTTP fetch defaults used in workflow placeholders
WEB_TIMEOUT_MS=8000
WEB_USER_AGENT=corestack-n8n-tools/1.0

# Optional search placeholders for 02-web-search-tool
SEARCH_API_URL=https://search.example/api
SEARCH_API_KEY=REQUIRED
