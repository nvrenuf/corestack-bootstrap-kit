# Required by Pack Contract v1.
#
# PORT_OFFSET is used as an operator convention when running multiple packs.
# Compose does not support arithmetic, so compute per-port values externally.
PORT_OFFSET=0

# Example port values (base 18000). When running multiple packs, adjust by PORT_OFFSET.
EXAMPLE_HTTP_PORT=18080

